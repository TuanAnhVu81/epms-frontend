import { Typography } from 'antd';

const { Title } = Typography;

// Placeholder — will be implemented in Phase 6
export default function PODetailPage() {
    return <Title level={2}>🔍 PO Detail — Coming Soon (Phase 6)</Title>;
}
