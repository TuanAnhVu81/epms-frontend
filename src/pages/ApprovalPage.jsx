import { Typography } from 'antd';

const { Title } = Typography;

// Placeholder — will be implemented in Phase 4
export default function ApprovalPage() {
    return <Title level={2}>✅ PO Approvals — Coming Soon (Phase 4)</Title>;
}
